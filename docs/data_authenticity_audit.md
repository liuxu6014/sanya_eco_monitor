# 数据真实性审计记录

审计时间：2026-05-09

## 审计范围

- 设备配置 ID：虫情、孢子、地表径流、雨量、水质、气象支撑。
- 数据入口：采集器、数据库表、汇总接口、专项分析接口、报告生成。
- 核查重点：是否使用真实设备 ID，是否混用设备，是否把累计表读数当成周期增量，是否有合成数据进入统计。

## 当前配置

- 虫情测报灯：`202603172301`
- 孢子捕捉仪：`202603172302`
- 地表径流监测系统：`16132920,16132921,16132922,16132923,16132924,16132925`
- 雨量计：`16132920,16132921,16132922`
- 面源水质污染负荷：`16133028`
- 历史气象坐标：`109.430,18.360`

## 已确认问题与修复

1. 雨量字段 `rainfall` 来自平台字段“雨量累计”，属于采集时刻累计读数，不能逐条累加。
   - 已改为按站点当天 `max - min` 得到日降雨量，再按站点平均得到区域日雨量。
   - 影响接口：`/api/sensor/rainfall/daily`、`/api/sensor/rainfall/analysis`、报告“监测期每日降雨量”。

2. 地表径流图表中的“当日降雨量”也使用了累计读数。
   - 已改为按站点当天 `max - min`。
   - 影响接口：`/api/sensor/runoff/daily`。

3. 报告径流汇总曾把高频采样值直接求和。
   - 已改为优先使用“累计流量”的区间差值；雨量同步使用区间差值。

4. 虫情和孢子采集曾自动补充合成 0 记录。
   - 已停止生成合成 0。
   - 历史合成记录保留在库中，但接口和报告统计已过滤，不作为真实采集数据。

5. 水质设备曾在配置设备当前周期无数据时回退到其他设备。
   - 已改为严格使用配置 ID `16133028`。
   - 若 `16133028` 在查询周期内无数据，返回无数据，不再使用 `16116030` 顶替。

6. 总览雨量卡片曾显示最新“雨量累计”原始读数。
   - 已改为显示当天累计增量，并保留 `raw_counter` 仅用于追溯。

## 本地数据库审计结果

- 虫情表：存在历史设备 `PBCR48F-340838-0001` 和当前配置设备 `202603172301`；近 30 天存在历史合成 0 记录。当前逻辑已只统计真实记录。
- 孢子表：近 30 天存在历史合成 0 记录。当前逻辑已只统计真实记录。
- 雨量表：`16132920,16132921,16132922` 三个站点均有高频累计读数。修复后最近 30 天累计降雨约 `72.0 mm`，峰值为 `2026-04-24 / 30.5 mm`。
- 水质表：库中同时存在 `16116030` 和 `16133028`。当前有效配置为 `16133028`，最新记录时间 `2026-04-24 14:35:09`，不会再使用 `16116030` 作为替代。
- 地表径流表：六个配置设备均有记录，流速、流量、水位、含沙量多为 0 或近 0；累计流量为区间差值口径。

## 仍需人工或第三方确认

- 传感器现场校准状态、安装位置、雨量筒堵塞、径流槽淤积、采样探头污染等问题，代码无法证明真实，只能通过现场巡检或平台原始数据对照确认。
- 水质 `16133028` 最新数据停在 `2026-04-24 14:35:09`。若平台当前应有新数据，需要检查该设备在线、采集任务和 WHXPH 平台返回。
- 气象支撑数据来自第三方接口，适合作为交叉参照，不应替代本地传感器实测。
